# Owner/source updates

## Screenshot available and read

User supplied accessible copy `/tmp/Screenshot 2026-09-19 at 10.28.00 PM.png`; main read successfully. Earlier Desktop EPERM no longer blocks visual review. Planner should inspect this copy.

Image: full-height split view; left ~40% width task table, right ~60% details. Left column headings TYPE PRI STATUS ID TITLE; highlighted selected title and left selection marker; dense rows, mixed open/in-progress/done, bottom page counter. Right title at top, metadata row, multiple heading/bullet sections, Description and Acceptance Criteria wrapping text. Bottom footer has navigation hints including Tab focus. Adopt split-list/detail interaction and structured detail hierarchy; no request for priorities, graph analysis, update banners, or bv unrelated features. User explicitly asks newest-first (do not assume screenshot ordering satisfies this).

## User clarification: modal, not full-height

User explicitly clarified screenshot is a reference for the LEFT/RIGHT PANE LOOK, not full-height sizing. A modal is fine. Prefer a bounded responsive modal with list/detail panes; no fullscreen requirement. Preserve list selection, readable task sections, scrolling and close/navigation hints within available modal height.

## Root owner message (2026-09-19)

Root source ahead of cfc773a0: j81 closed b7d92b68 (a9b0c391/e94c3d4a): canonical custom_message/intercom_message admitted with distinct role intercom; full sender header text+branchID/hash; no arbitrary custom/tools. Actual host trigger/steer/followUp passes; idle no-trigger lacks public extension hook, deferred next real turn (no polling).

6qv candidate62293575+470dab83: first completion batch includes one Jev focus Choice over all open tasks+none/concurrent/uncertain; full label data once in state; no first/newest heuristic; strict checkpointv6/no migration. Current pure presentationSnapshot()/debugSnapshot(), postcommit changed callback unchanged. Per-task historical health store/board projection does NOT exist, retainedcard single target only. Focus !=toolownership; evidenceLink remainsundefined. Scope currently message-driven focus, tools affect health only.

NEW tool-driven focus requirement is extra behavior beyond6qv; capture explicit actor/evidence/privacy/trigger contract (do not infer tool ownership from displayfocus). Worker solewriter currently fixes proven12byte capacity underbound; fullreview/publication stillpending. Main26+2focusreds green, overall326unit12localhost beforelatestcapacityred. Livefocus6/6JevPASS. Owner will send accepted finalrevision/seam. No root UI work; planning-only arch no runtime overlap.
