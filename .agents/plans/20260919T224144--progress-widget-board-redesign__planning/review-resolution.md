# Plan review resolution

Independent review c3b119ea returned BLOCK on draft. Main rewrote plan to resolve all 10 findings; no independent re-approval claimed, no implementation authorized.

1. Optional details blocking lifecycle: C commits mandatory work first, independently parsed optional candidates and bounded enrichment jobs; failure/capacity never semantic block.
2. Wrong-overlay teardown: architecture/A0/A4 use direct TUI showOverlay and exact handle.hide, no custom-overlay done promise.
3. Input transformations: contract explicitly pipeline-visible Right, no original-key guarantee; both listener orders tested.
4. Restore rebilling: A1 adds monitor/index/command seams and discriminated absent/restored/unsupported/corrupt, preserve data and zero save/dispatch for rejected restore.
5. Duplicate assistant focus: B reuses canonical message focus; only tool starts add inference, no message_end call.
6. Undefined batching: B specifies assistant tool-call set boundaries, all-member accumulation, overflow abstention, generations, replacement/preemption and expected call counts; host proof remains mandatory.
7. Whole-checkpoint admission: A2/B/C explicit predispatch byte proofs including cards/jobs/usage; optional denial isolated.
8. Health provenance: A2 persists exact source/observation/request/evidence identity, checks canonical amendments, Unassessed on mismatch.
9. Hidden warnings: normal two-row status precedence and board always-visible banner/provenance.
10. Unrequested dual-host support: only accepted-root pinned host supported; other inspected versions reference evidence only.

Other main corrections: no migration; exact user token line; separate working layers basic board → tool focus → optional details; tests main-owned; completed-cycle reset excluded; source refs superseded on accepted-root pin.

Validation performed: planning artifact/manual consistency check only; API ownership excerpt checked directly. No runtime edits, test runs, actual-host proofs or provider calls.
