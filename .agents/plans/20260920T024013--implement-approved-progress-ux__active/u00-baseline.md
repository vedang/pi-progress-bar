# U00 accepted baseline and frozen controls

- Root accepted runtime e4dcfe08, release0102cde4 / jj klvwuymrspvmxsrnwlkzouxvwmzqtvws. Arch exact requested rebase succeeded. Root repairs closed;84 records/25 UX children preserved. Main baseline format/check/378unit+12integration PASS.
- UX proof target installed Pi0.85.1, public TUI methods including showOverlay/handle.hide/getFocusedComponent. Local0.84.2 dependency still baseline, not an explicit two-version support promise. Host probe must use installed target.
- Monitor presentationSnapshot/debugSnapshot detached; changed publication after commit. No full board or health map. Core statuses not-started/reopened/done; five health fields retained on single card. Request counters already record actual dispatch; no task-derived count.
- Strictv6 caps200 tasks/20 included/1000events/512KiB; create event unique per task. Reuse durable event order if U03 replay/amendment tests confirm, rather than adding ordinal/checkpoint fields gratuitously. Health/projection work still needs schema review.
- OPEN/INPROG/DONE is presentation status only. OPEN not-started/reopened without fresh exclusive focus; INPROG fresh accepted exclusive semantic/tool focus; DONE actual recorded reported completion, qualified idle/retained when no newer work. No fabricated task for unknown/concurrent. Latest owner hints/count format preserved in plan and Beads. Two data rows plus hint row; token/request line selected only.
- Detail confidence>=.5 / selected probability>=.8 / yes are starting approved-plan policy, not calibrated fact. U15 disabled implementation; U16 owns real validation and explicit enabling before handoff.

## Corrections to scout report

Scout recommendations are evidence, not authority. Main read complete installed extensions.md3024lines, tui.md and keybindings.md plus relevant examples.
- pi.on(input) handles submitted prompts, NOT raw arrow keys. Use ctx.ui.onTerminalInput.
- Public TUI showOverlay exists in installed pi-tui/dist/tui.d.ts (227,311), handle methods175ff; docs' omission is not missing API. Generic ctx.ui.custom overlay done calls hideOverlay and remains rejected.
- Structural duck typing in pi-subagents is NOT adopted. Need known editor identity. Host startup sets focus to editor before extension initialization (interactive-mode.js647); fixture must prove exact host CustomEditor identity through extension loader and refusal around competing custom UI.
- Hint strings are view-local; no monitor persistence field needed for static text.
- U01 concerns host contracts only; widget status/count fixtures belong U07, state ordering U03. Do not pull later tickets forward.

## Next gate

U01 main-owned tests and static actual-host probe: belowEditor placement, canonical editor capture, pipeline Right guards/pass-through, direct owned overlay teardown including sibling above, listener detach/rebind, resize and custom-editor refusal. No provider calls. Host gaps block U02 rather than weakening guards.
